# TradeBot_XAU_Gemini

AI-powered trading bot for XAU/USD using OANDA and Google Gemini.

## Setup

1. `npm install`
2. Copy `.env.example` to `.env` and fill in your API keys.

## Running

- Demo Trading: `npm start`
- Run Backtest: `npm run backtest`
- Run Tests: `npm test`
