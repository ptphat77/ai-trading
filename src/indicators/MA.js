/**
 * Parameters read from config — never hardcoded here.
 */

function calculateSMA(data, period) {
  // Pure function for SMA
}

function calculateEMA(data, period) {
  // Pure function for EMA
}

function getCrossSignal(fastMA, slowMA) {
  // Return cross signal
}

module.exports = {
  calculateSMA,
  calculateEMA,
  getCrossSignal
};
