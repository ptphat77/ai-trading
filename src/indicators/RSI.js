/**
 * Parameters read from config — never hardcoded here.
 */

function calculate(data, period) {
  // Pure function for RSI
}

function getZone(rsiValue) {
  // Return Overbought, Oversold, or Neutral
}

module.exports = {
  calculate,
  getZone
};
