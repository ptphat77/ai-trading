/**
 * Builds prompt, calls Gemini API, validates and parses JSON response.
 * Returns skip on any failure.
 */
class GeminiAgent {
  constructor() {
    // Setup AI client
  }

  async decide(context) {
    // Analyze context and return decision
  }
}

module.exports = GeminiAgent;
